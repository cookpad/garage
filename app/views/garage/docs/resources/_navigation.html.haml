.well.sidebar-nav
  %ul.nav
    %li Resources
    - @documents.each do |document|
      - if document.visible_to?(_current_user)
        %li= link_to document.classified_name, resource_path(document.name)
